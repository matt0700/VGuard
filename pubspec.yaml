name: vguard
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+2
  tflite: ^1.1.2
  shared_preferences: ^2.0.8
  flutter_blue: ^0.8.0
  model_viewer_plus: ^1.8.0
  flutter_svg: ^1.0.0
  image_picker: ^1.1.2
  http: ^1.2.2
  flutter_cube: ^0.1.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/Car.glb
    - assets/Mercedes+Benz+GLS+580.glb
    - assets/vguard-logo.png
  uses-material-design: true
